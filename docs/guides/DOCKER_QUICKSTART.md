# Docker Quick Start

安全的 Docker 测试指南

---

## 🚀 快速开始（安全模式）

### 1. 创建配置文件

```bash
# 复制示例文件
cp .env.example .env

# 编辑配置（添加你的API密钥）
nano .env
```

### 2. 构建镜像

```bash
docker-compose build
```

### 3. 运行测试

```bash
# 仅本地测试（安全）
docker-compose up -d

# 查看日志
docker-compose logs -f

# 检查状态
docker-compose ps
```

### 4. 访问服务

```bash
# Web UI（仅本地访问）
open http://localhost:8080

# Gateway状态
curl http://localhost:8080/health
```

### 5. 停止服务

```bash
docker-compose down
```

---

## 🔒 安全说明

### ✅ 已实施的安全措施

1. **非root用户** - 容器以普通用户运行
2. **只读文件系统** - 防止恶意修改
3. **本地端口** - 只绑定到127.0.0.1
4. **资源限制** - CPU和内存限制
5. **无特权** - 删除所有特殊权限

### ⚠️ 安全风险

| 风险 | 缓解措施 | 状态 |
|------|---------|------|
| API密钥泄露 | 使用.env文件，不提交 | ✅ |
| 网络暴露 | 只绑定localhost | ✅ |
| 容器突破 | 非root + 只读 | ✅ |
| 资源耗尽 | 设置限制 | ✅ |
| 数据泄露 | 最小化卷挂载 | ✅ |

### 🛡️ 推荐做法

```bash
# 1. 确保.env不会被提交
git check-ignore .env  # 应该显示 .env

# 2. 验证端口绑定
netstat -tlnp | grep 18789  # 应该显示 127.0.0.1:18789

# 3. 检查用户权限
docker-compose run --rm clawdbot whoami  # 应该显示 clawdbot

# 4. 定期更新镜像
docker-compose build --no-cache
```

---

## 🧪 安全测试（无风险）

### 不使用真实API密钥测试

```bash
# 创建演示配置
cat > .env << 'EOF'
ANTHROPIC_API_KEY=demo-test-key
OPENAI_API_KEY=demo-test-key
CLAWDBOT_ENV=demo
EOF

# 运行状态检查（不需要真实密钥）
docker-compose run --rm clawdbot python -m clawdbot.cli status

# 清理
rm .env
docker-compose down
```

---

## ❓ 常见问题

### Q: 是否安全用于生产？
**A**: 不建议。这是测试配置，生产环境需要：
- 专业的密钥管理（AWS Secrets Manager等）
- 网络隔离和防火墙
- 身份验证和授权
- 安全监控

### Q: 端口会暴露到公网吗？
**A**: 不会。配置绑定到127.0.0.1，只能从本机访问。

### Q: API密钥是否安全？
**A**: 相对安全，但仍需注意：
- ✅ 不在Dockerfile中
- ✅ 使用.env文件
- ✅ .gitignore忽略
- ⚠️ 仍在容器环境变量中
- 🔒 生产环境需要密钥管理服务

### Q: 如何完全隔离网络？
**A**: 修改docker-compose.yml：
```yaml
networks:
  clawdbot-net:
    internal: true  # 完全隔离，无互联网访问
```

---

## 📚 更多信息

- **详细安全指南**: [DOCKER_SECURITY.md](DOCKER_SECURITY.md)
- **项目文档**: [README.md](README.md)
- **快速开始**: [QUICKSTART.md](QUICKSTART.md)

---

**⚠️ 重要提醒**：
- 仅用于本地测试和开发
- 不要在公网暴露端口
- 定期更新依赖
- 保护好API密钥

**安全级别**: 🟢 本地测试安全
