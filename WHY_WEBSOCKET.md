# 为什么 Gateway 需要 WebSocket？

> 解答：调用系统功能为什么不能用 HTTP，必须用 WebSocket？

---

## 核心问题

**疑问**：Gateway 的 85 个方法，大部分是系统管理（config.get、channels.status 等），这些不就是简单的函数调用吗？为什么需要 WebSocket？用 HTTP REST API 不就行了？

**答案**：因为 Agent 的特殊性 —— **流式输出** 和 **实时事件**。

---

## HTTP vs WebSocket 的本质区别

### HTTP：单向、短连接

```
客户端                     服务器
  │                          │
  ├─────── 请求 ────────────→│
  │     GET /api/config      │
  │                          │
  │←────── 响应 ──────────────┤
  │     { config: {...} }    │
  │                          │
  连接关闭                    连接关闭
```

**特点**：
- ✅ 简单、无状态
- ✅ 适合请求-响应模型
- ❌ 服务器无法主动推送
- ❌ 每次请求都要建立新连接

### WebSocket：双向、持久连接

```
客户端                     服务器
  │                          │
  ├──── 握手（HTTP升级）─────→│
  │                          │
  │←─── WebSocket 连接建立 ───┤
  │                          │
  ├─────── 消息 ────────────→│
  │     { method: "agent" }  │
  │                          │
  │←────── 事件 ──────────────┤
  │     { type: "thinking" } │
  │                          │
  │←────── 事件 ──────────────┤
  │     { type: "text" }     │
  │                          │
  │←────── 事件 ──────────────┤
  │     { type: "done" }     │
  │                          │
  持久连接保持                持久连接保持
```

**特点**：
- ✅ 双向通信
- ✅ 服务器可以主动推送
- ✅ 持久连接，无需重复握手
- ✅ 低延迟

---

## 为什么 Gateway 必须用 WebSocket？

### 原因 1：Agent 的流式输出

**Agent 处理一个消息可能需要 30 秒甚至更久**，期间会产生大量实时事件：

```python
# Agent 运行时产生的事件流
async for event in agent_runtime.run_turn(session, message):
    # 事件 1: 开始思考
    yield AgentEvent(type="thinking", text="我需要查询天气...")
    
    # 事件 2: 调用工具
    yield AgentEvent(type="tool_use", tool="weather", input={...})
    
    # 事件 3: 工具结果
    yield AgentEvent(type="tool_result", result="北京今天晴...")
    
    # 事件 4-20: 流式文本输出
    yield AgentEvent(type="text", text="北")
    yield AgentEvent(type="text", text="京")
    yield AgentEvent(type="text", text="今")
    yield AgentEvent(type="text", text="天")
    # ... 持续流式输出
    
    # 事件 21: 完成
    yield AgentEvent(type="done")
```

**如果用 HTTP**：
```
❌ 问题：
- HTTP 响应必须等所有内容都生成完才能返回
- 用户等待 30 秒看不到任何反馈
- 体验极差（就像 ChatGPT 要等 30 秒才一次性显示全部内容）

✅ WebSocket 方案：
- 每个事件立即推送给客户端
- 用户实时看到 Agent 的思考过程
- 看到文本逐字生成（打字机效果）
- 体验流畅
```

### 原因 2：双向实时通信

**场景**：用户在 Control UI 中与 Agent 对话，同时查看系统状态

```
时间线：

T=0s  客户端 → 服务器：agent({ message: "帮我分析日志" })
      ↓
T=1s  服务器 → 客户端：{ type: "thinking", text: "正在读取日志..." }
      ↓
T=3s  服务器 → 客户端：{ type: "tool_use", tool: "read_file" }
      ↓
T=5s  服务器 → 客户端：{ type: "text", text: "发现3个错误..." }
      ↓
T=10s 客户端 → 服务器：chat.abort()  ← 用户点击停止按钮
      ↓
T=11s 服务器 → 客户端：{ type: "aborted" }
```

**HTTP 无法做到这一点**：
- ❌ HTTP 请求发出后，无法在响应返回前发送新请求（中止）
- ❌ 需要客户端不断轮询（polling），浪费资源
- ✅ WebSocket 可以随时双向发送消息

### 原因 3：事件广播

**Gateway 的职责之一是广播系统事件**：

```python
# 场景：Telegram 用户发送消息

# 1. Telegram Bot 收到消息
telegram_bot.on_message(user_message)

# 2. Bot 调用 Agent
async for event in agent_runtime.run_turn(session, user_message):
    # 3. Bot 发送回复到 Telegram
    await telegram_api.send_message(chat_id, event.text)
    
    # 4. Gateway 同时广播事件到所有连接的客户端
    gateway.broadcast("chat", {
        "channel": "telegram",
        "chatId": chat_id,
        "event": event
    })

# 5. Control UI 实时看到这条对话（即使不是 Control UI 发起的）
# 6. CLI 工具也能监听到这个事件
# 7. iOS App 也能收到推送
```

**这是 HTTP 完全做不到的**：
- ❌ HTTP 无法主动推送
- ❌ 客户端需要每隔 1 秒轮询一次 `GET /events`（极其低效）
- ✅ WebSocket 服务器直接推送，0 延迟

---

## 技术对比

### 如果 Gateway 用 HTTP REST API

```python
# ❌ HTTP 方案的问题

# 客户端代码
async def call_agent(message):
    # 发送请求
    response = await http.post("/api/agent", json={"message": message})
    
    # ❌ 问题1：必须等待完整响应（30秒+）
    # ❌ 问题2：无法看到中间过程
    # ❌ 问题3：无法中途取消
    # ❌ 问题4：服务器无法主动推送其他 channel 的事件
    
    result = response.json()
    return result["text"]  # 30 秒后才能拿到

# 为了实时更新，客户端需要轮询
while True:
    events = await http.get("/api/events")  # 每秒请求一次
    for event in events:
        display(event)
    await asyncio.sleep(1)  # ❌ 浪费资源，且有延迟
```

### Gateway 使用 WebSocket

```python
# ✅ WebSocket 方案

# 客户端代码
async def call_agent(message):
    # 建立持久连接
    ws = await websocket.connect("ws://localhost:8765")
    
    # 发送请求
    await ws.send(json.dumps({
        "method": "agent",
        "params": {"message": message}
    }))
    
    # ✅ 实时接收事件流
    async for event in ws:
        data = json.loads(event)
        
        if data["type"] == "thinking":
            display_thinking(data["text"])  # 实时显示思考过程
        
        elif data["type"] == "text":
            display_text(data["text"])  # 逐字显示
        
        elif data["type"] == "done":
            break
        
        # ✅ 同时可以接收其他 channel 的广播事件
        elif data["event"] == "chat":
            display_other_channel_message(data)
    
    # ✅ 可以随时中止
    await ws.send(json.dumps({"method": "chat.abort"}))
```

---

## 实际案例对比

### 案例：调用 Agent 分析代码

#### HTTP 方案（不实用）

```bash
# 客户端发送请求
$ curl -X POST http://localhost:8000/api/agent \
  -d '{"message": "分析这个项目的架构"}'

# ⏳ 等待 45 秒...
# 👁️ 完全看不到进度
# 🤔 不知道 Agent 在做什么
# ❌ 无法中途取消

# 45 秒后一次性返回
{
  "text": "这个项目采用了三层架构：\n1. Gateway 层...\n2. Agent 层...\n3. Channel 层...",
  "duration": 45000
}
```

#### WebSocket 方案（实用）

```bash
# 客户端建立连接
$ wscat -c ws://localhost:8765

# 发送请求
> {"method": "agent", "params": {"message": "分析这个项目的架构"}}

# ✅ 实时接收事件（体验流畅）
< {"type": "thinking", "text": "我需要读取项目结构..."}  # T=1s
< {"type": "tool_use", "tool": "list_files"}               # T=2s
< {"type": "tool_result", "files": [...]}                  # T=3s
< {"type": "thinking", "text": "我看到了 Gateway、Agent、Channel..."}  # T=5s
< {"type": "text", "text": "这"}  # T=10s
< {"type": "text", "text": "个"}  # T=10.1s
< {"type": "text", "text": "项"}  # T=10.2s
< {"type": "text", "text": "目"}  # T=10.3s
# ... 逐字输出
< {"type": "done"}  # T=45s

# ✅ 用户随时可以按 Ctrl+C 或发送 abort
> {"method": "chat.abort"}
< {"type": "aborted"}
```

---

## Gateway 的 85 个方法 vs WebSocket

### 简单方法（如 config.get）

**疑问**：`config.get` 这种简单方法，为什么不能用 HTTP？

**答案**：
1. **统一接口**：Gateway 提供一个 WebSocket 端点，所有方法都通过同一连接
2. **避免混合协议**：如果 agent 用 WebSocket，config 用 HTTP，客户端需要维护两种连接
3. **权限管理**：WebSocket 连接建立时就验证权限，后续所有方法自动继承
4. **会话保持**：客户端连接后，所有请求都在同一个会话中

**实际上，这些简单方法也能用 HTTP**：

```python
# 技术上可行，但不实用
GET  /api/config          # 获取配置
GET  /api/channels/status # 查看 channels
GET  /api/sessions        # 列出 sessions

# 但是 agent 方法必须用 WebSocket
WS   ws://localhost:8765  # Agent 调用

# ❌ 问题：客户端需要维护两种连接
# ✅ 解决：全部用 WebSocket，统一管理
```

### WebSocket 的额外好处

即使是简单方法，WebSocket 也有优势：

```python
# HTTP 方案：每次请求都要建立连接
for i in range(100):
    response = await http.get("/api/config")  # ❌ 100 次 TCP 握手

# WebSocket 方案：一次连接，多次请求
ws = await websocket.connect("ws://localhost:8765")
for i in range(100):
    await ws.send({"method": "config.get"})  # ✅ 复用连接
```

---

## 总结

### 为什么 Gateway 必须用 WebSocket？

| 需求 | HTTP 能做到吗？ | WebSocket |
|------|----------------|-----------|
| Agent 流式输出 | ❌ 否（必须等完整响应） | ✅ 实时推送每个 token |
| 实时事件广播 | ❌ 否（需要轮询） | ✅ 服务器主动推送 |
| 双向通信（中止任务） | ❌ 难（请求发出后无法取消） | ✅ 随时发送新消息 |
| 长时间运行（30秒+） | ⚠️ 可以但容易超时 | ✅ 持久连接 |
| 多个客户端同步状态 | ❌ 需要频繁轮询 | ✅ 广播机制 |
| 统一接口 | ⚠️ 需要混合 HTTP+轮询 | ✅ 单一 WebSocket 连接 |

### 关键理解

**不是因为"系统管理功能"需要 WebSocket**，而是因为：

1. **Agent 的特性（流式输出、长时间运行）必须用 WebSocket**
2. **Gateway 为了统一接口，把所有方法都放在 WebSocket 上**
3. **这样客户端只需维护一个连接，既能调用 Agent，又能管理系统**

### 类比

```
传统 Web 服务：
  ├─ API 服务器（HTTP REST）      ← 适合简单请求-响应
  └─ 用户界面（HTTP + 轮询）      ← 效率低

OpenClaw Gateway：
  └─ WebSocket 统一端点
      ├─ Agent 流式输出           ← 必须 WebSocket
      ├─ 实时事件广播             ← 必须 WebSocket
      └─ 系统管理方法             ← 顺便也用 WebSocket（统一接口）
```

---

## 代码证据

### Control UI 的实现

```typescript
// openclaw/ui/src/ui/gateway.ts

export class GatewayClient {
  private ws: WebSocket;
  
  constructor(url: string) {
    // 建立 WebSocket 连接
    this.ws = new WebSocket(url);
    
    // 监听所有事件
    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data);
      
      // 处理 Agent 事件（流式输出）
      if (data.type === "text") {
        this.onAgentText(data.text);  // 逐字显示
      }
      
      // 处理广播事件（其他 channel 的消息）
      if (data.event === "chat") {
        this.onChatEvent(data);  // 实时更新聊天列表
      }
    };
  }
  
  // 调用 Agent（流式）
  async callAgent(message: string) {
    await this.ws.send(JSON.stringify({
      method: "agent",
      params: { message }
    }));
    // 响应会通过 onmessage 回调实时接收
  }
  
  // 获取配置（虽然简单，但也用 WebSocket）
  async getConfig(): Promise<Config> {
    const id = generateId();
    
    await this.ws.send(JSON.stringify({
      id,  // 请求 ID
      method: "config.get",
      params: {}
    }));
    
    // 等待响应（也是通过 WebSocket）
    return new Promise((resolve) => {
      this.pendingRequests[id] = resolve;
    });
  }
}
```

**关键点**：
- 所有方法（agent、config.get 等）都用同一个 WebSocket 连接
- Agent 的响应是流式的（多次 onmessage）
- 配置获取是单次响应，但也用 WebSocket（统一接口）

---

**总结一句话**：Gateway 用 WebSocket 不是因为"调用系统功能"需要，而是因为 **Agent 的流式输出和实时事件广播** 必须用 WebSocket，顺便把所有管理方法也统一到 WebSocket 上，简化客户端实现。
